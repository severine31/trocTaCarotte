{% extends 'base.html.twig'%}

{% block body %}
    <div class="containerBouton">
        <a class="btn btn-primary customBtn" href="./create" role="button">
            <img class=imgBouton src={{asset('build/image/legume/carotte_flat.png')}} style=width:2%/> 
            <div class="container">
                <span>Je troque ma carotte !</span>
            </div>
        </a>
    </div>
    <div class="container">
        <div class="row justify-content-md-center">
            {{ include("search.html.twig") }}
        </div>
    </div>
    <div class="container">
        <table class="table table-hover">
            <thead class="headerRow">
                <tr>
                    <th scope="col">'Carotte' à troquer</th>
                    <th scope="col">Contre</th>
                    <th scope="col">Informations</th>
                </tr>
            </thead>
            <tbody>
                {% for annonce in annonces %}
                    {% set vars = [{'carotte': annonce.CarotteATroquer }, {'Quantity': annonce.Quantity }, {'Unity': annonce.Unite}, {'Bio': annonce.Bio }, {'Image': annonce.CarotteATroquer.image }, {'Ville': annonce.ville }, {'contreCarotte': annonce.ContreCarotte }, {'contreQuantity': annonce.ContreQuantite }, {'contreUnity': annonce.ContreUnite}, {'contreBio': annonce.ContreBio }, {'ContreImage': annonce.ContreCarotte.image }] %}
                    {% include '_listingTable.html.twig' with vars %}
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}